{
  "name": "cicd-test",
  "version": "1.0.0",
  "private": true,
  "description": "用于测试 GitHub 上集成 CodeBuddy CLI 的 CI/CD 示例项目",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test"
  }
}
